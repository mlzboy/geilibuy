
<h1>Listing delivery_companies</h1>

<table>
  <tr>
    <th>Id</th>
    <th>Name</th>
    <th>Url</th>
    <th>Memo</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @delivery_companies.each do |delivery_company| %>
  <tr>
    <td><%= delivery_company.id %></td>
    <td><%= delivery_company.name %></td>
    <td><%= delivery_company.url %></td>
    <td><%= delivery_company.memo %></td>
    <td><%= link_to 'Show', admin_delivery_company_path(delivery_company) %></td>
    <td><%= link_to 'Edit', edit_admin_delivery_company_path(delivery_company) %></td>
    <td><%= link_to 'Destroy', admin_delivery_company_path(delivery_company), :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Delivery company', new_admin_delivery_company_path %>
