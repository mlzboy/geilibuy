
<h1>Listing jobs</h1>

<table>
  <tr>
    <th>Id</th>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @jobs.each do |job| %>
  <tr>
    <td><%= job.id %></td>
    <td><%= job.name %></td>
    <td><%= link_to 'Show', admin_job_path(job) %></td>
    <td><%= link_to 'Edit', edit_admin_job_path(job) %></td>
    <td><%= link_to 'Destroy', admin_job_path(job), :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Job', new_admin_job_path %>
